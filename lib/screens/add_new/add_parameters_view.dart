import 'package:flutter/material.dart';

class AddParametersView extends StatelessWidget {
  const AddParametersView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}